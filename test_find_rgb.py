import os
import sys

# find the path of adafruit_rgb_display
site_packages = "/home/pi/vtol-krti/.venv/lib/python3.13/site-packages"
print(f"I cannot access the pi directly. I must look at the code locally or via curl.")
